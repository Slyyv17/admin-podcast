import GetPodcast from "./getPodcast"

export default function getPodcastPage() {
    return (
        <main className=" min-h-screen w-full flex items-center justify-center">
            <GetPodcast />
        </main>
    )
}